# StillHasValue
Website and App

## Product photos

Do not commit full-resolution Google Photos / phone originals into `images/`.
Run `python3 scripts/optimize-shop-images.py` first (see `scripts/README.md`).
